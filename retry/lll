FROM debian:bookworm

RUN apt update && apt install -y mariadb-server

RUN mkdir -p /run/mysqld && chown mysql:mysql /run/mysqld

COPY script.sh .

CMD ["bash", "script.sh"]#!/bin/bash

mariadbd --user=mysql &

mariadb -u root <<EOF
create database if not exists wordpress;
create user if not exists '${DB_USER}'@'%' identified by 'password';
grant all privileges on wordpress.* to '${DB_USER}'@'%';
flush privileges;
EOF

mysqladmin -u root shutdown

exec mariadbd --user=mysqlFROM debian:bookworm

RUN apt update && apt install -y nginx 

CMD ["nginx", "-g", "daemon off;"]FROM debian:bookworm

RUN apt update && apt install -y php-fpm php php-mysql wget

CMD ["php-fpm8.2", "--nodaemonize"]FROM debian:bookworm

RUN apt update && apt install -y mariadb-server

RUN mkdir -p /run/mysqld && chown mysql:mysql /run/mysqld

COPY script.sh .

CMD ["bash", "script.sh"]#!/bin/bash

mariadbd --user=mysql &

until mariadb -u root -e "SELECT 1" > /dev/null 2>&1; do
    sleep 0.5;
done

db_password=$(cat /run/secrets/DB_USER_PASSWORD)

echo $db_password

mariadb -u root <<EOF
create database if not exists wordpress;
create user if not exists '${DB_USER}'@'%' identified by '$db_password';
grant all privileges on wordpress.* to '${DB_USER}'@'%';
flush privileges;
EOF

mysqladmin -u root shutdown

exec mariadbd --user=mysqlFROM debian:bookworm

RUN apt update && apt install -y nginx 

CMD ["nginx", "-g", "daemon off;"]FROM debian:bookworm

RUN apt update && apt install -y php-fpm php php-mysql wget

CMD ["php-fpm8.2", "--nodaemonize"]FROM debian:bookworm

RUN apt update && apt install -y mariadb-server

RUN mkdir -p /run/mysqld && chown mysql:mysql /run/mysqld

COPY script.sh .

CMD ["bash", "script.sh"]#!/bin/bash

mariadbd --user=mysql &

until mariadb -u root -e "SELECT 1" > /dev/null 2>&1; do
    sleep 0.5;
done

db_password=$(cat /run/secrets/DB_USER_PASSWORD)

echo $db_password

mariadb -u root <<EOF
create database if not exists wordpress;
create user if not exists '${DB_USER}'@'%' identified by '$db_password';
grant all privileges on wordpress.* to '${DB_USER}'@'%';
flush privileges;
EOF

mysqladmin -u root shutdown

exec mariadbd --user=mysqlFROM debian:bookworm

RUN apt update && apt install -y nginx 

CMD ["nginx", "-g", "daemon off;"]FROM debian:bookworm

RUN apt update && apt install -y php-fpm php php-mysql wget

CMD ["php-fpm8.2", "--nodaemonize"]services:
  nginx:
    image: nginx
    container_name: nginx
    build: ./requirements/nginx
    ports:
      - 443:443
    env_file:
      - ./.env
    networks:
      - inception

  mariadb:
    image: mariadb
    container_name: mariadb
    build: ./requirements/mariadb
    secrets:
      - DB_USER_PASSWORD
    env_file:
      - ./.env
    networks:
      - inception
    volumes:
      - mariadb_vol:/var/lib/mysql

  wordpress:
    image: wordpress
    container_name: wordpress
    build: ./requirements/wordpress
    env_file:
      - ./.env
    networks:
      - inception

networks:
  inception:

volumes:
  mariadb_vol:
    driver: local
    driver_opts:
      type: none
      device: /home/makkach/data/mariadb
      o: bind
    
secrets:
  DB_USER_PASSWORD: 
    file: ../secrets/DB_USER_PASSWORD
FROM debian:bookworm

RUN apt update && apt install -y mariadb-server

RUN mkdir -p /run/mysqld && chown mysql:mysql /run/mysqld

COPY script.sh .

CMD ["bash", "script.sh"]#!/bin/bash

mariadbd --user=mysql &

until mariadb -u root -e "SELECT 1" > /dev/null 2>&1; do
    sleep 0.5;
done

db_password=$(cat /run/secrets/DB_USER_PASSWORD)

echo $db_password

mariadb -u root <<EOF
create database if not exists ${DB_NAME};
create user if not exists '${DB_USER}'@'%' identified by '$db_password';
grant all privileges on ${DB_NAME}.* to '${DB_USER}'@'%';
flush privileges;
EOF

mysqladmin -u root shutdown

exec mariadbd --user=mysql --bind-address=0.0.0.0FROM debian:bookworm

RUN apt update && apt install -y nginx 

CMD ["nginx", "-g", "daemon off;"]FROM debian:bookworm

RUN apt update && apt install -y php-fpm php php-mysql wget

COPY script.sh .

CMD ["bash", "script.sh"]#!/bin/bash

wget https://wordpress.org/latest.tar.gz

tar -xzf latest.tar.gz

wget https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar

chmod +x wp-cli.phar

mv wp-cli.phar /usr/local/bin/wp

exec php-fpm8.2 --nodaemonizeservices:
  nginx:
    image: nginx
    container_name: nginx
    build: ./requirements/nginx
    ports:
      - 443:443
    env_file:
      - ./.env
    networks:
      - inception

  mariadb:
    image: mariadb
    container_name: mariadb
    build: ./requirements/mariadb
    secrets:
      - DB_USER_PASSWORD
    env_file:
      - ./.env
    networks:
      - inception
    volumes:
      - mariadb_vol:/var/lib/mysql

  wordpress:
    image: wordpress
    container_name: wordpress
    build: ./requirements/wordpress
    env_file:
      - ./.env
    networks:
      - inception
    volumes:
      - wordpress_vol:/wordpress

networks:
  inception:

volumes:
  mariadb_vol:
    driver: local
    driver_opts:
      type: none
      device: /home/makkach/data/mariadb
      o: bind
  wordpress_vol:
    driver: local
    driver_opts:
      type: none
      device: /home/makkach/data/wordpress
      o: bind
    
secrets:
  DB_USER_PASSWORD: 
    file: ../secrets/DB_USER_PASSWORD
FROM debian:bookworm

RUN apt update && apt install -y mariadb-server

RUN mkdir -p /run/mysqld && chown mysql:mysql /run/mysqld

COPY script.sh .

CMD ["bash", "script.sh"]#!/bin/bash

mariadbd --user=mysql &

until mariadb -u root -e "SELECT 1" > /dev/null 2>&1; do
    sleep 0.5;
done

db_password=$(cat /run/secrets/DB_USER_PASSWORD)

echo $db_password

mariadb -u root <<EOF
create database if not exists ${DB_NAME};
create user if not exists '${DB_USER}'@'%' identified by '$db_password';
grant all privileges on ${DB_NAME}.* to '${DB_USER}'@'%';
flush privileges;
EOF

mysqladmin -u root shutdown

exec mariadbd --user=mysql --bind-address=0.0.0.0FROM debian:bookworm

RUN apt update && apt install -y nginx 

CMD ["nginx", "-g", "daemon off;"]FROM debian:bookworm

RUN apt update && apt install -y php-fpm php php-mysql wget

COPY script.sh .

CMD ["bash", "script.sh"]#!/bin/bash

wget https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar

chmod +x wp-cli.phar

mv wp-cli.phar /usr/local/bin/wp

if [ ! -d /wordpress ]; then
    wp core download --path=/wordpress --allow-root
fi;

db_password=$(cat /run/secrets/WP_PASSWORD)

wp config create --dbname=${DB_NAME} --dbuser=${DB_USER} --dbhost=mariadb --dbpass=$db_password --path=/wordpress --allow-root

exec php-fpm8.2 --nodaemonizeservices:
  nginx:
    image: nginx
    container_name: nginx
    build: ./requirements/nginx
    ports:
      - 443:443
    env_file:
      - ./.env
    networks:
      - inception

  mariadb:
    image: mariadb
    container_name: mariadb
    build: ./requirements/mariadb
    secrets:
      - DB_USER_PASSWORD
    env_file:
      - ./.env
    networks:
      - inception
    volumes:
      - mariadb_vol:/var/lib/mysql

  wordpress:
    image: wordpress
    container_name: wordpress
    build: ./requirements/wordpress
    env_file:
      - ./.env
    networks:
      - inception
    volumes:
      - wordpress_vol:/wordpress
    secrets:
      - WP_PASSWORD

networks:
  inception:

volumes:
  mariadb_vol:
    driver: local
    driver_opts:
      type: none
      device: /home/makkach/data/mariadb
      o: bind
  wordpress_vol:
    driver: local
    driver_opts:
      type: none
      device: /home/makkach/data/wordpress
      o: bind
    
secrets:
  DB_USER_PASSWORD: 
    file: ../secrets/DB_USER_PASSWORD
  WP_PASSWORD:
    file: ../secrets/WP_PASSWORD
